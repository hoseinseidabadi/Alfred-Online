# رونوشت بگیر به `.env`. `.env` در .gitignore است — هرگز commit نشود.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/alfred_online?schema=public"
BRIDGE_KEY=""
SESSION_SECRET=""
PORT=3001
