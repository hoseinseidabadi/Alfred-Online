# رونوشت بگیر به `.env`. `.env` در .gitignore است — هرگز commit نشود.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/alfred_online?schema=public"
BRIDGE_KEY=""
SESSION_SECRET=""
PORT=3001

# پایگاه دادهٔ جداگانهٔ آزمون. اگر تنظیم نشود، آزمون‌های یکپارچه رد می‌شوند.
TEST_DATABASE_URL="postgresql://alfred:alfred_local_dev@127.0.0.1:5432/alfred_online_test?schema=public"
