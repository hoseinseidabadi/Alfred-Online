# Alfred-Online — سطحِ آنلاینِ مخزن دانش Alfred

سامانهٔ ثبت و تصمیم‌گیری درخواست‌های محصول آخرین خبر: ربات تلگرام برای ثبت، تریاژ با تعهد پاسخ
هفت‌روزه، صف ظرفیت، مایلستون، و داشبورد شورا.

**این پروژه سامانه‌ای مستقل نیست.** سطحِ آنلاینِ مخزن دانشی است که در ریپوی جداگانهٔ Alfred
زندگی می‌کند. جهت وابستگی یک‌طرفه است و هرگز برعکس نمی‌شود.

---

## نسبت با Alfred — مهم‌ترین چیزی که باید بدانی

|       | Alfred                                                | Alfred-Online (اینجا)            |
| ----- | ----------------------------------------------------- | -------------------------------- |
| مسیر  | `D:\AkharinKhabar\AKH APM`                            | `D:\AkharinKhabar\Alfred-Online` |
| ریپو  | `hoseinseidabadi/Alfred` (private)                    | این ریپو                         |
| محتوا | ۳۰۴ سند مارک‌داون — سرویس، فرآیند، رخداد، ایده، تحلیل | کد                               |
| نقش   | **منبع حقیقتِ دانش**                                  | منبع حقیقتِ **حالت عملیاتی**     |

### قاعدهٔ آهنین

> **این سامانه Alfred را فقط می‌خواند و هرگز در آن نمی‌نویسد** — نه خودکار، نه با تأیید انسان،
> نه در مسیر اضطراری. اصل I قانون اساسی.

دانشِ حاصل از تصمیم‌ها با دست و از راه کامندهای خود Alfred (`/add-event`، `/add-idea`،
`/update-service`) به آن برمی‌گردد.

### وقتی اینجا کار می‌کنی، Alfred را هم به نشست بده

کار کردن روی این ریپو بدون دانش Alfred کیفیت را پایین می‌آورد — تصمیم‌های این سامانه با ارجاع
به سرویس‌ها (`S-XX`)، فرآیندها (`P-XX`) و رخدادهای (`EV-XXX`) واقعی معنا پیدا می‌کنند.

```bash
claude --add-dir "D:\AkharinKhabar\AKH APM"
```

یا داخل نشست: `/add-dir "D:\AkharinKhabar\AKH APM"`.
یا در VS Code یک workspace چنددایرکتوری با هر دو پوشه باز کن.

**اسناد Alfred که بیشترین ارجاع را دارند:**

| سند                                                                         | چرا                                       |
| --------------------------------------------------------------------------- | ----------------------------------------- |
| `processes-and-flows/P-06-idea-intake/_overview.md`                         | فرآیندی که این سامانه پیاده‌اش می‌کند     |
| `processes-and-flows/P-06-idea-intake/Idea_Intake_Charter.md`               | منشور — سوال‌ها، SLA، شورا، مسیر سریع     |
| `processes-and-flows/P-06-idea-intake/Three_Scenarios_Walkthrough.md`       | سه سناریوی کامل + مدل عملیاتی             |
| `processes-and-flows/P-06-idea-intake/System_Workflow_Diagram.html`         | دیاگرام معماری                            |
| `processes-and-flows/P-06-idea-intake/dashboard/Intake_Dashboard.mock.html` | مرجع طراحی داشبورد                        |
| `services/INDEX.md` · `processes-and-flows/INDEX.md`                        | کاتالوگ `S-XX` و `P-XX` برای `serviceRef` |
| `Capabilities.md`                                                           | پروژه‌های خواهر و الگوهای قابل بازاستفاده |

---

## قانون اساسی — پیش از هر تصمیم طراحی بخوانش

`.specify/memory/constitution.md` (v1.0.0). هفت اصل، همه با MUST/MUST NOT:

1. **مرجعیت یک‌طرفهٔ دانش** — Alfred فقط‌خواندنی؛ سامانه بدون آن هم کار می‌کند
2. **دادهٔ خام دست‌نخورده می‌ماند** — پاسخ عین کاربر برای همیشه؛ مقادیر استخراجی جدا و نسخه‌دار
3. **لبه خودکفاست** — ثبت بدون هسته کامل می‌شود؛ شماره در لبه صادر می‌گردد
4. **تعهد پاسخ، ناوردای سامانه است** — هفت روز، با هشدار پیش از نقض
5. **هر تصمیم، متریک و تاریخ بازبینی** — مسیر سریع مسدود نمی‌شود ولی معاف هم نیست
6. **مرزهای ماژول از روز اول** — intake / dashboard / reports
7. **متناسب با مقیاس واقعی** — ۳۰–۵۰ ثبت در ماه، یک دولوپر پاره‌وقت

**پنج آزمون تاب‌آوری الزامی‌اند** و بر پیش‌فرض «تست‌ها اختیاری» غلبه دارند — بند
«تاب‌آوری و آزمون» قانون اساسی.

---

## معماری در یک نگاه

```
تلگرام ⇄ [Cloudflare Workers] ⇄ پلِ ناپایدار ⇄ [Liara] ⇄ داشبورد
              │                                      │
      Durable Object (گفت‌وگو)              PostgreSQL (منبع حقیقت)
      Durable Object (شمارنده)              NestJS + Next.js
      D1 (صف دوطرفه)
```

**چرا این تفکیک:** Liara سابقهٔ اختلال در ارتباط با خارج دارد (از جمله تلگرام)؛ Cloudflare در
همان بازه در دسترس بوده. چون اختلال **دوطرفه** است، پل ناپایدار فرض می‌شود و **تمام تماس‌ها از
سمت لبه آغاز می‌شوند** — هسته هرگز تماس خروجی به خارج نمی‌گیرد.

---

## کانونشن‌ها

- **زبان**: تمام متن رو به کاربر فارسی. تیتر و ساختار اسناد Spec Kit انگلیسی.
- **تاریخ**: ذخیره UTC، نمایش **جلالی و ساعت تهران**. هیچ تاریخ میلادی به کاربر نشان داده نمی‌شود.
- **اسرار**: هرگز در ریپو. فقط `.dev.vars` / `.env` محلی و Secret Store در استقرار.
- **ترتیب کار**: `spec` → `plan` → `tasks` → `implement`. بدون spec تأییدشده پیاده‌سازی شروع نمی‌شود.

---

<!-- SPECKIT START -->

## Active Feature

**`001-request-intake-triage`** — سامانهٔ ثبت و تصمیم‌گیری درخواست‌های محصول

- Plan: `specs/001-request-intake-triage/plan.md`
- Spec: `specs/001-request-intake-triage/spec.md`
- Research: `specs/001-request-intake-triage/research.md`
- Data model: `specs/001-request-intake-triage/data-model.md`
- Contracts: `specs/001-request-intake-triage/contracts/`
- Quickstart: `specs/001-request-intake-triage/quickstart.md`

**وضعیت**: فاز ۰ و ۱ کامل. `tasks.md` هنوز ساخته نشده — با `/speckit-tasks`.

**مسدودکننده‌های پیش از پیاده‌سازی**: S-1 (ادمین بودن ربات در کانال) · S-2 (اندازه‌گیری پایداری
پل) · S-3 (تصویب سازمانی P-06 و فاز ۰ دستی). جزئیات در `research.md` و `quickstart.md`.
<!-- SPECKIT END -->
